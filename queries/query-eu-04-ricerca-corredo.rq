PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT DISTINCT ?entity ?label WHERE {
  ?entity rdfs:label ?label .
  FILTER regex(?label, "corredo chirurgico|strumentario chirurgico|strumenti chirurgici|bisturi|specillo|scalpello", "i")
}
LIMIT 100
